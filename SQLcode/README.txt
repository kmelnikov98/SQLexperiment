Assuming that you have already an existing schema created within the SQL server,
you can take this code and use it sort the database with the 'Age' parameter.
The query should be manipulated as seen fit in order to link it best with your database.
